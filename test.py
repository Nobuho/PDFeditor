import img2pdf
import os
from glob import glob

pdfFileName = "output.pdf"
path = "C:\\_Python\\PDFeditor\\img"

# print(glob(path, recursive=True))  # 直下以下フォルダ全てを取得)
print(glob(path))  # 直下以下フォルダ全てを取得)
